DNS or (Domain name system) is a system that convert website names eg google.com into ip address  142.192.182.9 etc so our computer can understand where to go 

human can undersatand names
but pc cannot undersatnd names so it use ip adders for websites



Example :
         user wrote google.com
         DNS convert google.com into an ip address 
         and Browser can acces that web through that ip address 


what if i simple search :
        What is 2 + 2 ? 
        Then no DNS or ip involved  directly 
        but used indirectly eg Google search engine (google.com -> ip addreaa) done by DNS 
        We used search engine here
        so google search engine send request to server and server response
        and then it show the ans 